name: Publish

on:
  push:
   branches:
      - master

jobs:
  upload:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - uses: shallwefootball/s3-upload-action@master
        with:
          aws_key_id: ''
          aws_secret_access_key: ''
          aws_bucket: 'test123'
          source_dir: '../../entrenamiento'
