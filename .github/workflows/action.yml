name: Publish

on:
  push:
   branches:
      - master

jobs:
  upload:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - uses: shallwefootball/s3-upload-action@master
        with:
          aws_key_id: 'ASIASLBG62CD4A6CGTNG'
          aws_secret_access_key: '3IM23VxVQXJ5rnowtxHL1BljOM8rijqevEqNLY4j'
          aws_bucket: 'test123'
          source_dir: '../../entrenamiento'
